name: onekettled_bandit
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.12.2 <4.0.0' # Optimiert auf deine aktuelle Dart-Version

dependencies:
  flutter:
    sdk: flutter
  wakelock_plus: ^1.2.8
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
